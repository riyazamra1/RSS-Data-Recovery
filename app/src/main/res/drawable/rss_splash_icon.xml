<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Constrain the artwork to a compact centered 64dp splash icon. -->
    <item
        android:width="64dp"
        android:height="64dp"
        android:gravity="center">
        <bitmap
            android:src="@drawable/rss_data_recovery_splash_logo"
            android:gravity="center"
            android:filter="true" />
    </item>
</layer-list>
