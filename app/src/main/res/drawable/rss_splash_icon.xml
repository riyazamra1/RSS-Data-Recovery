<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Inset the artwork so Android 12+ system splash does not render the logo oversized. -->
    <item
        android:left="58dp"
        android:top="58dp"
        android:right="58dp"
        android:bottom="58dp">
        <bitmap
            android:src="@drawable/rss_data_recovery_splash_logo"
            android:gravity="center"
            android:filter="true" />
    </item>
</layer-list>
